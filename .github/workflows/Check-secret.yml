- name: Check secret
  run: |
    if [ -z "${{ secrets.SIGNING_SECRET }}" ]; then
      echo "Secret SIGNING_SECRET not found!"
      exit 1
    fi
    echo "Secret SIGNING_SECRET found: ${{ secrets.SIGNING_SECRET }}"
